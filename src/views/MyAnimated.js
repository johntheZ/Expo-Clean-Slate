import React from "react";
import { Text, ViewBase, View } from "react-native";

export const MyAnimated = ({
    ...props
}) => {
    return (
        <View style={{
            backgroundColor: 'red',
        }}>
            <Text>{"sda"}</Text>
        </View>
    )
}